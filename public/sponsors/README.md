# Sponsor logos

Drop square logos here (LinkedIn-style), then set `logo` in
`src/content/sponsors.ts`, e.g. `logo: "/sponsors/nvidia.png"`.

Recommended: square transparent PNG/SVG, ~256×256.
If `logo` is unset, a text monogram tile is shown instead.
